<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SelectRegisrationType"
    >
    <include layout="@layout/toolbar"
        android:id="@+id/appbar"

        />
<RelativeLayout
    android:padding="15dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_vertical">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/FuelStationType"
        style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="46dp"
        android:padding="12dp"
        android:text="MY fuel stations"
        android:textColor="@color/white"
        app:cornerRadius="10dp"
        app:icon="@drawable/ic_baseline_local_gas_station_24"
        app:iconGravity="textStart"
        app:rippleColor="@color/ripple" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/UserType"
        style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/FuelStationType"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="46dp"
        android:padding="12dp"
        android:text="Join the Queue"
        android:textColor="@color/white"
        app:cornerRadius="10dp"
        app:icon="@drawable/join_queue"
        app:iconGravity="textStart"
        app:rippleColor="@color/ripple" />
</RelativeLayout>
</RelativeLayout>